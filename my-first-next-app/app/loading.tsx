import React from "react";

/*
 * The loading component is used to display a loading message while the data is being fetched
 * and the loading.tsx is a special page that is used to display the loading component
 */

const Loading = () => {
  return <span className="loading loading-spinner loading-md"></span>;
};

export default Loading;
